#pragma once
/*
#include "Shift.h"
#include <map>


class BusTable {

private:

	std::multimap<unsigned int, unsigned int> driverId;
	std::multimap<unsigned int, unsigned int> lineId;
	std::multimap<std::vector<Shift>, unsigned int> schedule;

public:
	BusTable() {};
	//BusTable(unsigned int id, unsigned int driver, unsigned int line, vector<Shift> schedule);
	BusTable* insert(unsigned int id, unsigned int driver, unsigned int line, std::vector<Shift> schedule);
	

	unsigned int getOrderId(unsigned int driver);
	unsigned int getLineId(unsigned int driver);
	std::vector<Shift> getSchedule(unsigned int driver);


};

*/