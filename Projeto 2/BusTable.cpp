/*
#include "BusTable.h"
#include <utility>



BusTable* BusTable::insert(unsigned int id, unsigned int driver, unsigned int line, std::vector<Shift> schedule) {
	driverId.insert(std::pair<unsigned int, unsigned int>(driver, id));
	lineId.insert(std::pair<unsigned int, unsigned int>(line, id));
	this->schedule.insert(std::pair<std::vector<Shift>, unsigned int>(schedule, id));

	//Ver se esta correto
	/////////////////////
	return this;
	/////////////////////
}
*/
